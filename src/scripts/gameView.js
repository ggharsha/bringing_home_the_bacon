import Game from "./game";

export default class GameView {
    constructor(ctx) {
        this.game = x // new Game(ctx);
        this.ctx = ctx;
    }

    start() {

    }

    bindKeyHandlers() {
        // keyhandlers
    }
}